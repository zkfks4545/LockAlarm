# LockAlarm 변경 내역

이 문서는 사용자 피드백을 반영해 통합 APK에 적용한 주요 패치를 정리합니다. 앱 표시 이름은 **LockAlarm**, 최신 앱 버전은 **0.15.7**이며, 현재 설치 파일은 `outputs/routine-alarm-integrated-final-v15.7-debug.apk`입니다. 1.0.0 전까지 APK 파일명은 `0.xx.xx` 버전에서 `v<앞자리 없는 버전>-debug` 형식을 사용합니다.

## 0.15.7 — 화면 잠금 타이머 켜짐/꺼짐

- 알람 편집 화면에 화면 잠금 타이머 `켜짐/꺼짐` 선택을 추가했습니다.
- 켜짐 상태의 지연 시간은 `0~60초`로 직접 조절할 수 있습니다.
- 꺼짐 상태는 지연 시간을 `0초`로 저장하며, 알람 시작 즉시 종료·스누즈·위로 밀기 조작을 사용할 수 있습니다.
- 기존 Room 알람은 5→6 마이그레이션으로 타이머 상태를 보존하고, 기존 지연 시간이 있으면 켜짐으로 가져옵니다.

### 검증

- `testDebugUnitTest` 97건 통과, 실패/오류 0건
- `lintDebug` 오류 0건, `assembleDebug` 성공
- APK SHA-256: `609615DCC44308E06BCC93D160CD5D5B2FD45F11EB6F429EB8C550AC175E5A82`
- APK: `outputs/routine-alarm-integrated-final-v15.7-debug.apk`

## 0.15.6 — 화면 크기 변경 시 영상 위치 보존

- 폴드 접기·펼치기나 넓은 화면·좁은 화면 전환 중 `SurfaceView`가 잠시 `0초`를 반환해 마지막 재생 위치를 덮어쓰던 문제를 수정했습니다.
- 실제 재생 중이고 길이와 위치가 유효할 때만 로컬 영상 위치를 저장하고, 표면 교체 시 마지막 정상 위치를 세션에 전달합니다.
- 음원 `MediaPlayer`와 알람 세션은 새로 시작하지 않으므로 사용자가 듣던 소리 재생 위치·음량 램프·타이머가 화면 전환으로 초기화되지 않습니다.

### 검증

- `testDebugUnitTest` 96건 통과, 실패/오류 0건
- `lintDebug` 오류 0건, `assembleDebug` 성공
- APK SHA-256: `BBED58A8C30170247F7C5CE95F87E7F1C66F5CB7680C333BC757077C4874A54C`
- APK: `outputs/routine-alarm-integrated-final-v15.6-debug.apk`

## 0.15.5 — 알람 종료 버튼 스타일 통일

- 울림 화면의 실제 종료 버튼을 원형 `×` 대신 5분 스누즈와 같은 반투명 둥근 버튼으로 변경했습니다.
- 버튼 문구는 `알람끄기`로 표시하고, 타이머가 끝나기 전에는 기존처럼 비활성 상태를 유지합니다.
- 잠금화면 Activity와 홈 복구 오버레이에 동일하게 적용됩니다.

### 검증

- `testDebugUnitTest` 96건 통과, `lintDebug`, `assembleDebug` 성공
- APK SHA-256: `49E1DAE7AD1008DF58AE575F198624E8AA8CA174B14B0397DD5B0F29533AABE4`

## 0.15.4 — 앱 표시 이름 변경

- Android 런처와 앱 정보 화면의 표시 이름을 `LockAlarm`으로 변경했습니다.
- 알람 채널과 내부 패키지 식별자는 기존 동작을 유지합니다.

### 검증

- `testDebugUnitTest`, `lintDebug`, `assembleDebug` 성공
- APK SHA-256: `E8A866D7CECBE9B3DCE6FFFAF92806375CF7DEDB44B7568229FE92388025487A`

## 0.15.3 — 내비게이션·편집 뒤로가기 보정

- 좁은 화면 하단 내비게이션을 3개 동일 폭으로 배치해 `타이머` 탭이 화면 밖으로 밀리지 않도록 했습니다.
- 하단 내비게이션에 시스템 내비게이션 영역 인셋을 적용해 탭이 기기 작업 표시줄/제스처 영역에 가려지지 않도록 했습니다.
- 넓은 화면 레일과 본문에도 Scaffold 인셋을 적용해 마지막 `타이머` 항목과 콘텐츠가 하단 시스템 영역 아래로 들어가지 않도록 했습니다.
- 알람 편집 화면에서 시스템 뒤로가기 버튼을 누르면 상단 `뒤로`와 동일하게 편집 화면으로 돌아갑니다.

### 검증

- JVM 단위 테스트 96건 통과 (`testDebugUnitTest`)
- `lintDebug` 성공 (오류 0건; 기존 경고 36건·힌트 8건)
- `assembleDebug` 성공
- APK SHA-256: `2061F7D71AECC9B7D6AC9F4D279F0843A472FFA4FD4D7AE355A863CCBA5C9750`

## 0.15.2 — 알람 음량 점진 상승

- 로컬 음악, 오디오 트랙이 있는 로컬 영상, 기기 기본 알람 URI를 `MediaPlayer`로 재생할 때 개별 gain을 기본 10%에서 시작해 30초 동안 250ms 간격으로 100%까지 점진적으로 올립니다.
- 램프는 `MediaPlayer.setVolume`만 사용하며, 알람 시작 시 한 번 적용하는 기기 `STREAM_MUSIC` 초기값을 실행 중 다시 덮어쓰지 않습니다.
- 서비스는 재생 객체·세션 ID·generation을 함께 확인해 오래된 콜백이 새 세션의 음량을 바꾸지 않도록 하고, 최종 해제·스누즈·선점·서비스 종료 전에 램프 콜백을 제거합니다.
- `MediaPlayer` 생성·재생이 실패하면 기존 기본 알람 URI 및 `ToneGenerator` 대체 경로를 유지합니다. `ToneGenerator` 대체음은 고정 음량이며 점진 상승 대상이 아닙니다.

### 검증

- JVM 단위 테스트 96건 통과 (`testDebugUnitTest`)
- `lintDebug` 성공
- `assembleDebug` 성공
- APK SHA-256: `E8106DC65CABCD120F93202E4AE3622478E34F188ABC20919ED802FA5A0BBB9E`

## 0.15.1 — 카드 경계·YouTube 썸네일·다중 로컬 영상

- 여러 카드의 로컬 영상이 동시에 무음 재생되며, 부모보다 크게 측정하지 않는 표준 VideoView 표면과 카드별 클리핑으로 영상이 다른 카드 영역을 침범하지 않습니다.
- 카드 로컬 영상은 표면 크기를 카드 안에 고정하고 명시적인 중앙 크롭 변환을 적용해 원본 비율을 유지한 채 카드 영역을 채웁니다. 비율 차이로 넘치는 부분만 잘라 표시하며 영상 표면 크기는 카드 경계를 넘지 않습니다.
- 권한 설정 직후 다시 구성되는 홈 카드에서 읽기 권한이 사라진 로컬 영상 URI도 poster 또는 안내 화면으로 대체하고, `VideoView` 생성·준비·오류 예외가 앱 전체를 종료시키지 않도록 보호합니다.
- 홈 화면의 YouTube 콘텐츠는 공식 썸네일만 표시하고 IFrame 재생은 알람 울림 화면에서만 사용합니다.
- 영상 자체 소리는 홈 미리보기에서 계속 음소거하고 알람 울림 화면의 소리 정책과 분리합니다.

### 검증

- JVM 단위 테스트 93건 통과 (`testDebugUnitTest`)
- `lintDebug` 성공
- `assembleDebug` 성공
- APK SHA-256: `8209BB3A979215446A1BF71898619504AC84EBF1D9835E467F703EC11AE735A6`

## 0.13.1 — 카드 미리보기 표면 통일

- 상단 대표 알람과 아래 알람 카드의 미디어·시간·알람명·편집·삭제·알람/미리보기 스위치를 동일한 오버레이 카드 UI로 통일했습니다.
- 넓은 화면 2열 레이아웃에서도 각 카드가 고정된 190dp 높이와 자체 클리핑 경계를 사용해 다른 카드 영역을 침범하지 않습니다.
- 작은 로컬 영상 카드는 첫 프레임 포스터를 표시하고 대표 카드만 무음 반복 재생해 다중 VideoView 디코더 충돌을 피합니다.

### 검증

- JVM 단위 테스트 93건 통과 (`testDebugUnitTest`)
- `lintDebug` 성공
- `assembleDebug` 성공
- APK SHA-256: `567680C04830DE2F67631C452FC8AD382A005EC2A518DDE4FBB299F01DD0519A`

## 0.13.0 — 알람별 미리보기와 타이머

- 각 알람 카드의 활성화 스위치 아래에 독립적인 `미리보기` 스위치를 추가했습니다. 알람 스케줄 revision이나 예약에는 영향을 주지 않고 Room에 알람별 상태를 저장합니다.
- 로컬 이미지·GIF·WebP·영상은 카드 미리보기 영역에서 재생하고, 유효한 YouTube URL은 공식 썸네일을 누르면 카드 안 공식 IFrame 플레이어로 전환됩니다. 카드 목록은 동시에 하나의 YouTube 미리보기만 재생하며 다른 카드 재생·미리보기 OFF·목록 이탈 시 이전 WebView를 제거합니다. 미리보기 스위치를 끄면 카드 미디어 영역을 숨깁니다.
- 여러 로컬 영상을 카드에 동시에 배치할 때 기기 디코더가 첫 VideoView만 표시하는 문제를 피하기 위해 대표 카드 외의 영상 카드는 첫 프레임 포스터를 표시합니다.
- 스톱워치 옆에 초 단위 입력·시작·일시정지·계속·초기화를 제공하는 타이머 탭을 추가했습니다. `SystemClock`을 기준으로 화면 이동 중에도 남은 시간을 유지합니다.
- 알람 목록은 활성/비활성 상태를 우선하지 않고 설정한 시각이 이른 순서로 항상 정렬됩니다.
- Room 4→5 마이그레이션으로 `homePreviewEnabled`를 추가했으며 기존 알람의 기본값은 켜짐입니다.

### 검증

- JVM 단위 테스트 93건 통과 (`testDebugUnitTest`)
- `lintDebug` 성공
- `assembleDebug` 성공
- APK SHA-256: `1EC0EB3EA507B9C8C8602B8AE3DA22B302CD983F1276F7A59E62FDA0B1118B45`

## 0.12.0 — 잠금화면형 알람 울림 UI

- 메인 알람 목록을 반응형 카드 대시보드로 개편했습니다. 좁은 화면은 세로 카드와 하단 플로팅 내비게이션, 넓은 화면은 좌측 레일과 2열 카드로 표시하며 첫 활성 알람은 큰 강조 카드로 보여줍니다.
- 다크·라이트 테마를 상단 버튼으로 즉시 전환하고 선택값을 앱에 저장합니다. 알람 울림 전체화면과 미디어 재생 동작은 기존대로 유지합니다.

- 잠금화면 `AlarmRingActivity`와 홈 복구용 `TYPE_APPLICATION_OVERLAY`가 공통 `AlarmOverlayController` 표면을 사용하도록 UI를 통합했습니다.
- 로컬 영상·이미지와 YouTube를 전체 화면 배경으로 유지하고, 상단 중앙에 매분 갱신되는 `HH:mm`과 알람 이름을 표시합니다.
- 우측 상단 `CircularDismissView`는 남은 초·진행률과 완료 상태만 표시하는 전용 표시기이며, 타이머 전후 모두 터치로 해제하지 않습니다.
- 타이머 전에는 화면 전체 입력 차단막과 하단 잠금 손잡이로 종료·스와이프·스누즈·미디어 조작을 모두 막습니다.
- 타이머 완료 뒤 하단 좌측 5분 스누즈(최초 울림 1회), 우측 큰 X, 160dp 이상 수직 위 스와이프 종료를 활성화했습니다. 짧거나 수평 우세 드래그는 거부합니다.
- X와 스와이프는 기존 세션 ID를 포함한 `AlarmPlaybackService.dismissIntent(...)`로 요청하고, 스누즈도 서비스에서 세션·시간·1회 정책을 검증합니다.
- 화면 복구·회전·Activity/오버레이 전환은 새 세션이나 새 `ringStartedAtMillis`를 만들지 않습니다.
- 하단 손잡이의 잠금 해제 문구가 실제 `unlockAtMillis` 기준으로 5·4·3초처럼 정확히 갱신되도록 수정했습니다. 완료 시 `위로 밀어 알람 종료`로 전환됩니다.
- MIME이 비어 있거나 `application/octet-stream`·제조사 MIME인 MediaStore 영상도 파일 확장자로 분류하고, duration이 없어도 영상 목록에 표시합니다. 음악의 기존 duration 필터는 유지합니다.
- 새 이미지·GIF·WebP·영상 선택 시 이전 `audioUri`를 제거하고 영상 자체 소리 또는 기본음 정책을 다시 계산하도록 공통 순수 helper를 적용했습니다.
- 알람 편집 화면의 유효한 YouTube URL 아래에 공식 썸네일과 재생 버튼을 표시하고, 탭하면 기존 공식 IFrame 플레이어로 재생하도록 했습니다. 미리보기 닫기, 빈 URL·지원하지 않는 URL 안내, URL 변경 시 WebView 정리를 포함하며 다운로드·캐시·MP3 추출은 하지 않습니다.
- Galaxy 영상 목록 조회를 `MediaStore.Video`와 외부 `MediaStore.Files` 컬렉션으로 넓히고 `SIZE`·`IS_PENDING` SQL 필터를 제거했습니다. MIME·파일명·duration·날짜가 비어 있거나 비정상이어도 가능한 행을 보존하고, Android 14 선택 영상 권한과 권한 미허용·조회 실패·진짜 0건 안내를 구분합니다. 직접 찾아보기는 잘못된 MIME 영상도 선택할 수 있습니다.
- Android 14에서 선택한 영상 권한만 있는 경우 `현재 선택한 영상만 접근 가능하며 전체 목록은 보이지 않습니다` 안내와 `전체 영상 접근 허용` 버튼을 표시하고, 기존 두 권한 재요청 경로로 전체 접근을 다시 요청합니다. Android 사진 선택기와 직접 찾아보기는 계속 사용할 수 있습니다.

### 검증

- JVM 단위 테스트 90건 통과 (`testDebugUnitTest`)
- `lintDebug` 성공
- `assembleDebug` 성공
- APK SHA-256: `D0C1299BC58CB172683627245000A0B7BFB1B125C74EF2E01F475270A8270EDD`

## 0.11.0 — 확장자 목록과 시작 권한 설정

- MediaStore MIME 정보가 비어 있거나 일반 형식으로 표시되는 파일도 파일 확장자를 확인해 목록에 표시합니다.
- 지원 확장자는 이미지·GIF·WebP·영상·음악 탭으로 자동 분류하고 PDF 등 미지원 파일은 제외합니다.
- 앱 실행 시 필수 알람 접근 설정 화면을 먼저 표시합니다.
- 모든 필수 접근이 완료되기 전에는 새 알람 만들기, 알람 편집, 테스트를 사용할 수 없습니다.
- 기존 알람과 최근 콘텐츠는 `권한 설정 없이 기존 목록만 보기`로 계속 열람할 수 있습니다.

### 검증

- JVM 단위 테스트 72건 통과
- Android lint 오류 0건
- APK SHA-256: `2D3FCF7507B1D7600C768536507B35C0558807ADE1CEABB499ED6DC44E9581CD`

## 0.10.0 — 기기 미디어 보관함

- 이미지·GIF·WebP·영상·음악을 분리한 5개 탭 미디어 선택 화면을 추가했습니다.
- 이미지·영상은 기기 MediaStore의 최신 썸네일을 격자로 표시합니다.
- 음악은 파일명·재생 길이·미리듣기·선택 동작이 있는 목록으로 표시합니다.
- 모든 탭에 최근순·이름순 정렬과 대소문자를 구분하지 않는 파일명 검색을 제공합니다.
- 앱이 실제로 지원하는 MIME 형식만 목록에 남기고 재생 길이가 없거나 비어 있는 미디어는 제외합니다.
- Android 사진 선택기를 시각 콘텐츠의 별도 선택 경로로 유지합니다.
- MediaStore에 없는 다운로드·클라우드 파일은 `직접 찾아보기`로 선택할 수 있습니다.
- 선택한 파일은 복사하거나 경로를 저장하지 않고 기존과 동일하게 콘텐츠 URI만 알람에 저장합니다.
- 기기 미디어 열람 권한을 거부해도 Android 사진 선택기와 직접 찾아보기는 계속 사용할 수 있습니다.

### 검증

- JVM 단위 테스트 71건 통과
- Android lint 오류 0건
- APK SHA-256: `ADCFBB90DBCB52A757F4FD56A577F1991DCB2E806BFF29D34EBD61B30B795C7C`

## 0.9.0 — 적응형 화면과 재생 연속성

### 화면 방향·폴드 대응

- 일반 YouTube 영상은 가로, 명시적인 Shorts URL은 세로를 초기 방향으로 사용합니다.
- 플레이어 준비 뒤에는 전체 방향 센서를 허용해 기기 회전에 따라 가로·세로로 전환합니다.
- 로컬 이미지·영상은 회전 메타데이터를 반영한 실제 표시 비율로 초기 방향을 결정합니다.
- 방향 전환과 갤럭시 폴드 접기·펼치기 같은 화면 크기 변경에서는 알람 Activity를 재생성하지 않고 기존 재생 화면을 다시 측정합니다.

### 잠금화면·오버레이 UI 통합

- 잠금화면 Activity와 다른 앱 위 오버레이가 같은 네이티브 재생 화면 생성 코드를 사용합니다.
- 알람 이름, 반투명 스누즈, 원형 진행 타이머와 X의 디자인·크기·위치를 통일했습니다.
- 잠금화면에서 스누즈가 불투명한 시스템 기본 버튼 형태로 바뀌던 차이를 제거했습니다.

### 화면 복구 시 이어보기

- 세션별 재생 위치와 현재 재생 화면 소유권을 관리합니다.
- 새 화면이 재생을 이어받기 전에 이전 화면의 마지막 위치를 저장하고 중복 재생을 멈춥니다.
- 로컬 영상은 0.25초 간격, YouTube는 공식 IFrame API 안에서 약 0.5초 간격으로 위치를 인계합니다.
- 단순 화면 복구·회전·폴드 변경은 새 알람 시작으로 처리하지 않으므로 해제 타이머, 스누즈 횟수, 밝기·음량 스냅샷과 초기값 적용 상태가 유지됩니다.

### 필수 접근 게이트

- 정확한 알람, 시스템 밝기 변경, 전체화면 알람, 다른 앱 위 표시, 알림 접근이 모두 준비되기 전에는 새 알람·편집·전체화면 테스트 진입을 차단합니다.
- 누락된 접근 이름과 각 설정 버튼을 표시합니다.
- 설정 화면이나 알림 권한 요청에서 돌아오면 즉시 상태를 다시 확인합니다.
- 편집 중 필수 접근이 회수되면 편집을 종료하고 권한 안내가 있는 알람 목록으로 돌아갑니다.
- 기존 알람 목록과 최근 콘텐츠의 열람·삭제는 계속 허용합니다.

### 검증

- JVM 단위 테스트 64건 통과
- Android lint 오류 0건
- APK SHA-256: `205CC3FC10307C9C807D3645E11F0D04E418A97A3AA5451D5482360948E70342`

## 0.8.0 — 이탈 방지와 입력 차단

- 알람 Activity에서 뒤로가기를 무시하도록 변경했습니다.
- 홈·최근 앱·작업 제거 뒤 포그라운드 서비스가 활성 세션을 확인하고 전체화면 오버레이를 복구합니다.
- 화면 켜짐과 잠금 해제 신호에서 잠금 상태에 맞는 Activity 또는 오버레이를 다시 표시합니다.
- 화면 전환이나 Activity 종료를 알람 최종 해제로 처리하지 않도록 분리했습니다.
- 종료 지연 중 로컬 영상과 YouTube 위에 전체화면 입력 차단막을 적용했습니다.
- 지연 중에는 영상 조작과 X를 막고 최초 울림의 1회 스누즈만 허용합니다.
- 지연 완료 뒤 차단막을 제거하고 원형 진행 표시를 활성 X로 전환합니다.
- 최종 해제는 서비스가 현재 세션과 해제 가능 시각을 검증한 X 요청에서만 수행합니다.
- JVM 단위 테스트 52건, Android lint 오류 0건을 확인했습니다.

## 0.7.0 — 영상 우선 전체화면 배치

- YouTube 화면 위에 별도로 높이를 차지하던 고정 조작 행을 제거했습니다.
- 로컬·YouTube 영상이 전체 화면을 먼저 사용하도록 변경했습니다.
- 알람 이름은 좌측, 스누즈와 원형 X는 우측의 비율 여백 또는 영상 가장자리 위에 겹쳐 표시합니다.
- 조작 UI 때문에 영상의 사용 가능 높이와 가로폭이 함께 줄어들던 문제를 해결했습니다.
- JVM 단위 테스트 45건, Android lint 오류 0건을 확인했습니다.

## 0.6.0 — 원본 비율 최대 맞춤

- 실제 울림 Activity와 오버레이 모두 중앙 확대·잘라내기 대신 원본 비율 맞춤을 사용하도록 변경했습니다.
- 회전 메타데이터가 적용된 영상 크기를 기준으로 화면 안에 들어가는 최대 픽셀 크기를 계산합니다.
- 영상을 자르거나 늘이지 않으며, 화면과 영상의 비율 차이로 불가피한 최소 여백만 남깁니다.
- 가로·세로·동일 비율 경계 계산 테스트를 추가해 JVM 단위 테스트가 45건으로 늘었습니다.

## 0.5.0 — 편집 UI·미디어 선택·최근 콘텐츠 정리

- 시·분 숫자 직접 입력과 1~3600초 테스트 간격 입력을 추가했습니다.
- 한 번 알람에서 오늘 이미 지난 시각을 선택하면 날짜를 내일로 자동 보정합니다.
- 선택한 이미지·GIF·영상·음악의 파일명을 표시하고 시각 미리보기와 음악 미리듣기를 제공합니다.
- 소리 선택 버튼을 제거하고 `별도 로컬 음악 → 영상 자체 소리 → 기기 기본 알람음` 순서로 자동 결정합니다.
- YouTube WebView의 HTTPS Referer·origin을 앱 ID로 교정하고 임베드 금지·비공개·식별 실패 안내를 구분했습니다.
- 예약·울림·스누즈·해제 같은 실행 이벤트 로그 저장을 제거했습니다.
- 최근 사용한 로컬 파일과 YouTube URL만 중복 없이 30개까지 저장하는 최근 콘텐츠 화면을 추가했습니다.
- 로컬 파일은 열람·미리보기만 제공하고 YouTube 항목은 URL 복사를 제공합니다.
- JVM 단위 테스트 41건, Android lint 오류 0건을 확인했습니다.

## 초기 통합 단계 — MVP 및 v2~v4 산출물

- Kotlin·Jetpack Compose 단일 Android 앱 모듈을 구성했습니다.
- Room 기반 복수 알람, 기존 SharedPreferences 알람 1회 이관, 단발·요일 반복·포함/제외 날짜를 구현했습니다.
- `AlarmManager.setAlarmClock()` 정확 예약, 고유 PendingIntent, 부팅·앱 교체·시각·시간대·권한 변경 후 재등록을 연결했습니다.
- `scheduleRevision`, `occurrenceId`, `sessionId`와 Room 트랜잭션 선점으로 중복·오래된 전달을 거부합니다.
- 현재 울림 중 새 정규 알람은 FIFO로 대기시키고, 스누즈 대기 중 새 정규 알람은 기존 스누즈를 종료한 뒤 우선 실행합니다.
- 알람 시작 순간 밝기·자동 밝기 모드·미디어 음량을 저장하고 지정 초기값을 한 번만 적용합니다.
- 최종 해제 때 알람별 복원 또는 현재 값 유지 정책을 적용합니다.
- 최초 울림에서 발생당 한 번, 누른 시점부터 정확히 5분 뒤 재울림하는 동일 세션 스누즈를 구현했습니다.
- 포그라운드 서비스가 로컬 음원을 소유하고 영상 자체 소리·별도 음악·기본 대체음을 반복 재생합니다.
- 잠금화면 Activity, 전체화면 알림, 다른 앱 위 오버레이, 화면 켜짐 유지 경로를 연결했습니다.
- YouTube 외부 이관 방식에서 공식 IFrame API 기반 앱 내부 플레이어로 변경했습니다.

## 현재 남은 실기기 검증

- Galaxy 잠금화면, Doze, 재부팅, OEM 절전, 권한 회수
- 홈·최근 앱·화면 꺼짐과 다시 켜짐 뒤 전체화면 복구
- Galaxy Fold 접기·펼치기와 다중 화면 크기 변경
- 로컬·YouTube 화면 인계 시 영상 위치와 음성 연속성
- 손상 또는 접근 권한이 사라진 미디어와 YouTube 네트워크·자동재생 제한
- TalkBack, 200% 글꼴, 색 대비

물리 전원 버튼과 Android 설정의 강제 종료는 일반 앱이 차단할 수 없습니다. YouTube 이어보기와 자동재생은 공식 플레이어 정책과 네트워크 상태의 영향을 받습니다.
